{
  "name": "@vault-core/hook-claude-code",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bunx tsc --build",
    "dev": "bunx tsc --watch",
    "typecheck": "bunx tsc --noEmit",
    "install-hooks": "bun run src/install.ts"
  },
  "dependencies": {
    "@vault-core/core": "workspace:*",
    "@vault-core/types": "workspace:*"
  }
}
