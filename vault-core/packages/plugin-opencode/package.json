{
  "name": "@vault-core/plugin-opencode",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/plugin.ts",
  "scripts": {
    "build": "bunx tsc --build",
    "dev": "bunx tsc --watch",
    "typecheck": "bunx tsc --noEmit",
    "install-plugin": "bun run scripts/install-plugin.ts"
  },
  "dependencies": {
    "@opencode-ai/plugin": "*",
    "@vault-core/core": "workspace:*",
    "@vault-core/types": "workspace:*"
  }
}
